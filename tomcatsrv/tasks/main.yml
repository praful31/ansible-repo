---
# tasks file for tomcatsrv
- name: Update Repos On Node
  apt:
    update_cache: yes
  

- name: Install Packages
  apt:
    name: "{{item}}"
    state: latest
  loop:
      - unzip
      - openjdk-8-jdk
    
    